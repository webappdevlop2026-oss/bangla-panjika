# আপডেট নির্দেশিকা — বাংলা পঞ্জিকা (ShriPanchang)

**প্যাকেজ:** `com.shripanchang.calendar`
**সর্বশেষ সংশোধন:** ১৬ সেপ্টেম্বর ২০২৬

প্রতিটি আপডেটের আগে এই ফাইলটা একবার পড়ে নিন। এখানে লেখা আছে কোন
ফিচার চালু করতে কোন লাইনে হাত দিতে হবে, আর Play Store কী কী কারণে
আটকাতে পারে।

---

## ১. প্রতিটি আপডেটে যা করতেই হবে

এই তিনটে ভুললে আপলোড আটকে যাবে।

### ক) versionCode বাড়ান

`pubspec.yaml` এর ১৯ নম্বর লাইন:

```yaml
version: 1.0.0+1
                ↑ এই সংখ্যাটা প্রতিবার বাড়াতেই হবে
```

`1.0.0+1` → `1.0.1+2` → `1.0.2+3` … এভাবে।

**+ চিহ্নের পরের সংখ্যাটাই versionCode।** আগের রিলিজের চেয়ে বড় না
হলে Play Console বলবে *"Version code X has already been used"*।

### খ) একই keystore দিয়ে সই

`android/key.properties` ফাইলটা যেন থাকে:

```
storePassword=...
keyPassword=...
keyAlias=key0
storeFile=../com.shripanchang.calendar.jks
```

> ⚠️ `com.shripanchang.calendar.jks` আর তার পাসওয়ার্ড **কখনো হারাবেন
> না**। হারালে অ্যাপটা আর কোনোদিন আপডেট করা যাবে না — নতুন নামে নতুন
> অ্যাপ বানানো ছাড়া উপায় থাকবে না, পুরনো ৫০০+ ইউজারও হারিয়ে যাবেন।
>
> দুই জায়গায় ব্যাকআপ রাখুন। `.gitignore`-এ আগে থেকেই বাদ দেওয়া আছে।

### গ) বিল্ড ও যাচাই

```
dart format .\lib\main.dart
flutter analyze .\lib\main.dart          ← "error -" থাকলে থামুন
flutter build appbundle --release
flutter run --release                     ← ফোনে খুলে দেখুন
```

**release বিল্ড ফোনে খুলে না দেখে কখনো আপলোড করবেন না।** debug-এ চললেই
release-এ চলবে, এমন নয় — আগে একবার release APK খুলতেই বন্ধ হয়ে গিয়েছিল
(R8 কোড ছেঁটে ফেলেছিল বলে)।

---

## ২. কোন ফিচার চালু করতে কোথায় হাত দিতে হবে

প্রতিটাই এক-দুই লাইনের কাজ। ফিচারের কোড আগে থেকেই লেখা আছে, শুধু
মানটা বসালেই নিজে থেকে চালু হয়ে যাবে।

| ফিচার | ফাইল | কী বদলাবেন |
|---|---|---|
| **বিজ্ঞাপন** | `lib/main.dart` ~৮১৪ | `bannerAdUnitId` ও `interstitialAdUnitId` — আসল AdMob আইডি |
| | `AndroidManifest.xml` | `com.google.android.gms.ads.APPLICATION_ID` |
| **জ্যোতিষী বুকিং** | `lib/main.dart` ~১১৪৩ | `_astrologerWhatsApp` — নম্বর (দেশের কোড সহ, + ছাড়া) |
| **ক্লাউড ব্যাকআপ / API** | `lib/main.dart` ~৯১০ | `BackendConfig.hosted` — যেমন `https://api.ডোমেইন.com` |
| **প্রিমিয়াম চালু** | `lib/main.dart` ~১৪৫ | `freeForEveryone = false` |

### কীভাবে বানানো আছে

- **বিজ্ঞাপন** — টেস্ট আইডি বসানো থাকলে কোনো বিজ্ঞাপনই দেখায় না
  (`realAdsConfigured` মিথ্যা)। আসল আইডি বসালেই নিজে থেকে চালু।
- **জ্যোতিষী** — নম্বর খালি থাকলে "শীঘ্রই আসছে" পর্দা দেখায়। নম্বর
  বসালেই বুকিং ফর্ম ফিরে আসে।
- **backend** — `hosted` খালি থাকলে রিলিজ বিল্ডে খালি ফেরত যায়, আর
  ক্লাউড ব্যাকআপ স্পষ্ট বার্তা দেখায়। URL বসালেই সব চালু।

---

## ৩. Play Store যেসব কারণে আটকাতে পারে

### এখনকার রিলিজে ঝুঁকি নেই — তবে জেনে রাখুন

| বিষয় | অবস্থা |
|---|---|
| targetSdk 36 | ✅ বসানো (৩১ অগাস্ট ২০২৬ থেকে বাধ্যতামূলক) |
| `SCHEDULE_EXACT_ALARM` | ✅ লাগছে না — রিমাইন্ডার `inexactAllowWhileIdle` ব্যবহার করে। এই অনুমতিটা Google-এর কড়া নজরে, এড়িয়ে যাওয়াই ভালো |
| debug key | ✅ আর ব্যবহার হচ্ছে না |
| `com.example` প্যাকেজ | ✅ বদলানো |
| অ্যাকাউন্ট মোছার ব্যবস্থা | ✅ লাগে না — অ্যাপে কোনো অ্যাকাউন্টই নেই |

### Data safety ফর্মে যা বলতে হবে

| প্রশ্ন | উত্তর |
|---|---|
| তথ্য সংগ্রহ করেন? | **হ্যাঁ** — Location, Personal info (নাম, জন্ম তারিখ), Audio |
| তথ্য শেয়ার করেন? | **না** (বিজ্ঞাপন চালু করলে → হ্যাঁ, Advertising) |
| এনক্রিপ্টেড? | **হ্যাঁ** |
| ইউজার মুছতে পারেন? | **হ্যাঁ** — আনইনস্টল করলেই সব মুছে যায় |

**অনুমতির কারণ:**

| অনুমতি | কারণ |
|---|---|
| `ACCESS_FINE_LOCATION` | জেলা অনুযায়ী সূর্যোদয়/অস্ত ও আবহাওয়া |
| `RECORD_AUDIO` | শুধু ভয়েস রিমাইন্ডারে — কথা লেখায় বদলাতে, রেকর্ডিং রাখা হয় না |
| `POST_NOTIFICATIONS` | ব্যবহারকারীর সেট করা রিমাইন্ডার |
| `RECEIVE_BOOT_COMPLETED` | রিস্টার্টেও রিমাইন্ডার বহাল রাখতে |
| `INTERNET` | আবহাওয়া ও মনীষীদের পরিচয় |

> `RECORD_AUDIO` Google সবচেয়ে খুঁটিয়ে দেখে। Data safety-তে কারণটা
> স্পষ্ট লিখবেন, আর অ্যাপে অনুমতি চাওয়ার আগে ব্যবহারকারীকে জানানো
> হয় — এটাই যথেষ্ট।

---

## ৪. ⚠️ ভবিষ্যতের তিনটে ফাঁদ

এগুলো এখন সমস্যা নয়, কিন্তু ওই ফিচারগুলো যোগ করার সময় **অ্যাপ সরিয়ে
দেওয়া হতে পারে** যদি না মেনে চলেন।

### ফাঁদ ১ — প্রিমিয়াম পেমেন্ট

**Razorpay দিয়ে অ্যাপের ভেতরের প্রিমিয়াম বিক্রি করা যাবে না।**

Google-এর নিয়ম: অ্যাপের ভেতরের ডিজিটাল সাবস্ক্রিপশন **Google Play
Billing** দিয়েই বিক্রি করতে হয়। Google ১৫-৩০% কাটে। না মানলে অ্যাপ
সরিয়ে দেওয়া হয়।

Razorpay চলবে শুধু **অ্যাপের বাইরের সেবায়** — যেমন জ্যোতিষীর সঙ্গে
সামনাসামনি বসার ফি, বা কোনো ছাপা বই।

কোডে Razorpay-র ব্যবস্থা আছে, ওটা নষ্ট করার দরকার নেই — শুধু প্রিমিয়াম
আনলকের জন্য ব্যবহার করবেন না।

### ফাঁদ ২ — অ্যাকাউন্ট যোগ করলে

এখন অ্যাপে কোনো অ্যাকাউন্ট নেই, তাই কোনো শর্ত নেই।

কিন্তু "account recovery" বা লগইন যোগ করলেই Google-এর **Account
Deletion নীতি** চালু হয়ে যাবে:

- অ্যাপের ভেতরে অ্যাকাউন্ট মোছার ব্যবস্থা রাখতে হবে
- **ওয়েবেও একটা পাবলিক লিংক** দিতে হবে যেখান থেকে অ্যাকাউন্ট মোছা যায়
- Play Console-এ সেই লিংকটা জমা দিতে হবে

এটা না থাকলে আপডেট রিজেক্ট হয়।

### ফাঁদ ৩ — জ্যোতিষ ও স্বাস্থ্য

জ্যোতিষী পরামর্শে "স্বাস্থ্য" বিভাগ আছে। **রোগ সারানো বা চিকিৎসার
দাবি করা যাবে না** — Google স্বাস্থ্য-সংক্রান্ত দাবি কড়াভাবে দেখে।

"স্বাস্থ্য ও গৃহ পরামর্শ" লেখা থাকলে ঠিক আছে। কিন্তু "রোগমুক্তি",
"আরোগ্য" জাতীয় কথা লিখবেন না।

---

## ৫. ভবিষ্যতে বিল্ড ভাঙতে পারে

আজকের বিল্ডে এই সতর্কবার্তা এসেছে:

```
Your app uses the following plugins that apply Kotlin Gradle Plugin (KGP):
flutter_tts, home_widget, share_plus, speech_to_text.
Future versions of Flutter will fail to build if your app uses plugins
that apply KGP.
```

**আজ কিছু হবে না।** কিন্তু Flutter আপগ্রেড করলে একদিন বিল্ড ভেঙে যেতে
পারে। তখন ওই চারটে প্যাকেজের নতুন সংস্করণে যেতে হবে।

সেই সঙ্গে **৪৬টি প্যাকেজের নতুন সংস্করণ** বেরিয়েছে। বড় আপডেটের সময়
`flutter pub outdated` চালিয়ে ধাপে ধাপে আপগ্রেড করবেন — একসঙ্গে সব
নয়, নইলে কী ভাঙল বোঝা যাবে না।

---

## ৬. প্রতিটি আপডেটের ধাপ

```
১. কোড বদলান
২. pubspec.yaml এ versionCode বাড়ান
৩. dart format + flutter analyze
৪. flutter build appbundle --release
৫. flutter run --release  → ফোনে খুলে দেখুন
৬. Play Console → Production → Create new release
৭. AAB আপলোড
৮. "What's new" লিখুন (বাংলায়)
৯. বিজ্ঞাপন চালু করলে → Store listing এ "অ্যাপে বিজ্ঞাপন আছে" = হ্যাঁ
১০. Send for review
```

Google-এর রিভিউ সাধারণত কয়েক ঘণ্টা থেকে ২-৩ দিন।

---

## ৭. ফাইল কোথায় কী আছে

| ফাইল | কী |
|---|---|
| `play-listing.md` | Play Console-এ কপি-পেস্ট করার সব লেখা |
| `privacy-policy.html` | গোপনীয়তা নীতি (ডোমেইনে তুলতে হবে) |
| `play_icon_512.png` | Play Store-এর আইকন |
| `feature_graphic_1024x500.png` | ফিচার গ্রাফিক |
| `android/com.shripanchang.calendar.jks` | সই করার চাবি — **হারাবেন না** |
| `android/key.properties` | চাবির পাসওয়ার্ড — git-এ যায় না |
| `android/app/proguard-rules.pro` | R8 চালু করতে চাইলে দরকার |
