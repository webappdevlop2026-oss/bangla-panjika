<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- আবহাওয়া/পেমেন্ট (Razorpay) সহ যেকোনো নেটওয়ার্ক কলের জন্য প্রয়োজন -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- রিমাইন্ডারের নোটিফিকেশন দেখানোর জন্য (Android 13+ এ ব্যবহারকারীর অনুমতি লাগে) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- ফোন রিস্টার্ট হলেও সেট করা রিমাইন্ডার যেন বহাল থাকে -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- ফোনের real-time লোকেশন ধরে সূর্যোদয়/অস্ত, আবহাওয়া ও বৃষ্টি সঠিকভাবে দেখানোর জন্য -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- ভয়েস রিমাইন্ডার ফিচারে কথা শুনে টেক্সটে বদলানোর জন্য (speech_to_text) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:label="বাংলা পঞ্জিকা"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- বিজ্ঞাপনের জন্য AdMob App ID — এটা Google-এর অফিসিয়াল টেস্ট ID,
             নিজের AdMob একাউন্ট থেকে আসল App ID পেলে এটা বদলে দিন
             (README.md এ বিস্তারিত ধাপ আছে) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- নির্ধারিত সময়ে রিমাইন্ডার বাজানোর জন্য (flutter_local_notifications) -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- হোম স্ক্রিন উইজেট — আজকের বাংলা তারিখ/তিথি দেখায় -->
        <receiver
            android:name=".PanjikaWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/panjika_widget_info"/>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- জ্যোতিষী বুকিং-এ WhatsApp/ফোন কল খোলার জন্য (url_launcher,
             Android 11+ এ package visibility-র জন্য এগুলো লাগে) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- ভয়েস রিমাইন্ডারে speech_to_text প্যাকেজ ফোনের স্পিচ-রিকগনিশন
             সার্ভিস খুঁজে পেতে (Android 11+ package visibility-র জন্য) -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
