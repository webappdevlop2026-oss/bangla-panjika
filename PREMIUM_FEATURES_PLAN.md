# ১০টি প্রিমিয়াম ফিচার — পরিকল্পনা ও রোডম্যাপ

এই ডকুমেন্টটা লেখার আগে পুরো `lib/main.dart` (১৪,০০০+ লাইন) ও `pubspec.yaml` খুঁটিয়ে দেখা হয়েছে। ফলাফল: চাওয়া ১০টা ফিচারের মধ্যে **৩টা ইতিমধ্যেই সম্পূর্ণ তৈরি আছে**, **৩টা আংশিক তৈরি আছে** (existing অংশ বাড়ালেই হবে), আর বাকি **৪টা সত্যিই নতুন**, যার মধ্যে ২টা (AR Sky, Live Astronomical Sky) বড় কাজ।

তাই সবচেয়ে efficient রাস্তা হলো: আগে যেগুলো আধা-তৈরি সেগুলো শেষ করা (কম সময়ে বড় ভ্যালু), তারপর মাঝারি কাজগুলো, সবার শেষে ভারী hardware-নির্ভর ফিচার দুটো।

---

## বর্তমান আর্কিটেকচার (সংক্ষেপে)

- পুরো অ্যাপ একটাই ফাইলে (`lib/main.dart`) — স্ক্রিন, লজিক, ডেটা সব একসাথে।
- পঞ্চাঙ্গ হিসাব: `PanchangCalculator` ক্লাস — sidereal longitude + Lahiri ayanamsa (`ayanamsa()`, `_ayanamsaJ2000 = 23.8531`) ব্যবহার করে তিথি/নক্ষত্র/রাশি/সূর্যোদয়-অস্ত/রাহুকাল বের করে। এটা কার্যত "দৃক সিদ্ধান্ত" (আধুনিক জ্যোতির্বিদ্যা-ভিত্তিক) পদ্ধতি — কোনো "সূর্য সিদ্ধান্ত" মোড এখনো নেই।
- অবস্থান/আবহাওয়া: `LocationService`, `WeatherService` (GPS না পেলে ম্যানুয়াল জেলায় ফিরে যায়) — `geolocator`, `http` প্যাকেজ দিয়ে।
- রিমাইন্ডার: `ReminderStore` + `flutter_local_notifications` — টেক্সট + তারিখ-সময় দিয়ে বানানো যায়, ভয়েস ইনপুট নেই।
- শেয়ার/কার্ড: `FestivalPosterScreen`-এ ইতিমধ্যেই RepaintBoundary → PNG ইমেজ → `Share.shareXFiles` প্যাটার্ন কাজ করছে (উৎসব পোস্টার মেকার)। আজকের পঞ্চাঙ্গের জন্য আলাদা প্লেইন-টেক্সট শেয়ারও আছে (`buildTodayPanchangSummary` + `Share.share`)।
- হোম উইজেট: `home_widget` প্যাকেজ দিয়ে ইতিমধ্যে Android লঞ্চার-উইজেট যোগ করা আছে।
- পারিবারিক ক্যালেন্ডার: `FamilyCalendarScreen` + `FamilyMemberStore` — জন্মদিন/বিবাহবার্ষিকী ইতিমধ্যে যোগ করা যায়।
- হোম স্ক্রিনে "আজ/কাল" বক্স: `_TodayTomorrowBoxes` — এইমাত্র যোগ করা হয়েছে।
- শুভ মুহূর্ত: `ShubhoMuhurtaScreen` + `BengaliCalendarData.findAuspiciousMuhurtas()` — কাজ (বিবাহ/গৃহপ্রবেশ/জমি-বাড়ি-গাড়ি কেনা ইত্যাদি) বেছে নিলে পরবর্তী শুভ দিন-সময় দেখায়।

---

## ফিচার-বাই-ফিচার অবস্থা

| # | ফিচার | বর্তমান অবস্থা | নতুন প্যাকেজ লাগবে? |
|---|---|---|---|
| ৫ | Bengali Calendar Widget | ✅ **সম্পূর্ণ আছে** (`home_widget`) | না |
| ৬ | Family Calendar | ✅ **সম্পূর্ণ আছে** (`FamilyCalendarScreen`) | না |
| ৭ | আজ+কাল Smart Cards | ✅ **সম্পূর্ণ আছে** (`_TodayTomorrowBoxes`, এইমাত্র যোগ হলো) | না |
| ৪ | Smart Muhurat Finder | 🟡 **~৮৫% আছে** — কাজের ধরন বাছাই + পরবর্তী শুভ সময় খুঁজে দেয়। শুধু "কোন সময়ের মধ্যে" (deadline) বাউন্ড নেই — এখন খুঁজলে যতদূর লাগে ততদূর যায়, একটা "শেষ তারিখ" ফিল্টার নেই। | না |
| ১০ | Panchang Share Card | 🟡 **আংশিক** — কার্ড-থেকে-ছবি বানানোর ইঞ্জিন (`FestivalPosterScreen`) আগে থেকেই আছে, শুধু "আজকের পঞ্চাঙ্গ/রাশি/মুহূর্ত"-এর জন্য নিজস্ব ডিজাইনের কার্ড বানিয়ে একই ইঞ্জিনে বসাতে হবে। | না |
| ৮ | Festival Countdown + Share | 🟡 **আংশিক** — `upcomingFestivals()` উৎসবের তালিকা দেয়, কিন্তু "আর X দিন বাকি" সংখ্যাটা গণনা করে দেখানো হয় না, আর প্রতিটা উৎসবের নিজস্ব শেয়ার-বাটনও নেই। | না |
| ৯ | Voice Reminder | 🔴 **নতুন** — বলে রিমাইন্ডার বানানো, কোনো স্পিচ-রিকগনিশন কোড নেই (শুধু TTS আছে, উল্টোটা STT নেই) | **হ্যাঁ** — `speech_to_text` |
| ৩ | দুটি পঞ্জিকা পদ্ধতি | 🔴 **নতুন** — এখন শুধু দৃক সিদ্ধান্ত আছে; সূর্য সিদ্ধান্তের জন্য আলাদা জ্যোতির্বিদ্যা সূত্র (ভিন্ন ayanamsa rate/epoch) বসাতে হবে | না (pure math) — তবে ঝুঁকিপূর্ণ কাজ |
| ২ | Live Astronomical Sky | 🔴 **নতুন** — গ্রহ/চাঁদের পজিশন হিসাবের গণিত অলরেডি আছে (`sunLongitude`, `rashiIndexFor` ইত্যাদি), কিন্তু ফোন ঘোরালে আকাশের সাথে মিলিয়ে দেখানোর কম্পাস/জাইরো অংশ নেই | **হ্যাঁ** — `sensors_plus` |
| ১ | AR Sky (ক্যামেরা) | 🔴 **নতুন, সবচেয়ে ভারী** — ক্যামেরা + কম্পাস + গ্রহ-পজিশন গণিত একসাথে মেলাতে হবে | **হ্যাঁ** — `camera` + `sensors_plus` (দরকার হলে `ar_flutter_plugin`/ARCore) |

---

## প্রস্তাবিত ধাপ (Phase)

### Phase 1 — দ্রুত, কম ঝুঁকি, নতুন প্যাকেজ লাগে না (প্রথমে এগুলো করা উচিত)
এই তিনটা existing কোডের ওপর ছোট সংযোজন, তাই সবচেয়ে কম সময়ে সবচেয়ে বেশি প্রিমিয়াম ফিল দেবে:

1. **Festival Countdown + Share (#৮)** — `upcomingFestivals()`-এ "আর কত দিন বাকি" সংখ্যা যোগ করা, হোম স্ক্রিনের উৎসব কার্ডে সেটা দেখানো, আর প্রতিটা কার্ডে একটা শেয়ার-বাটন বসানো (existing পোস্টার-ইমেজ ইঞ্জিন পুনঃব্যবহার করে)।
2. **Panchang Share Card (#১০)** — আজকের তিথি/নক্ষত্র/রাশি/শুভ সময় দিয়ে একটা নতুন, সুন্দর ডিজাইনের কার্ড (existing পোস্টার থিমের মতো) বানিয়ে হোম স্ক্রিন ও রাশি/মুহূর্ত স্ক্রিন থেকে এক-ট্যাপে শেয়ার করার অপশন।
3. **Smart Muhurat Finder উন্নতি (#৪)** — এখনকার স্ক্রিনেই "কত দিনের মধ্যে চাই" (যেমন ৭/১৫/৩০ দিন) একটা ফিল্টার যোগ করা।

### Phase 2 — মাঝারি কাজ, একটাই নতুন প্যাকেজ প্রতি ফিচারে
4. **Voice Reminder (#৯)** — `speech_to_text` যোগ করে, existing রিমাইন্ডার স্ক্রিনেই একটা মাইক-বাটন বসানো। বাংলা বলা কথা টেক্সটে বদলে "টাইটেল" ফিল্ডে বসিয়ে দেবে; তারিখ/সময় এখনো ম্যানুয়ালি বেছে নিতে হবে (প্রথম ভার্সনে পুরোপুরি "বলে বলে সব সেট" না করে, আগে বেসিক ট্রান্সক্রিপশনটা ঠিকভাবে কাজ করানো ভালো — নাহলে ভুল বুঝে ভুল সময়ে রিমাইন্ডার সেট হওয়ার ঝুঁকি থাকে)।
5. **দুই পঞ্জিকা পদ্ধতি (#৩)** — এটা সবচেয়ে সাবধানে করতে হবে, কারণ এটা সরাসরি existing Panchang calculation-কে ছোঁয়। পরিকল্পনা:
   - `PanchangCalculator`-এর বর্তমান পদ্ধতি (Lahiri ayanamsa) অক্ষত রেখে, তার পাশে একটা আলাদা "Surya Siddhanta ayanamsa/constants" ফাংশন যোগ করা।
   - Settings-এ একটা টগল ("দৃক সিদ্ধান্ত" / "সূর্য সিদ্ধান্ত") — ডিফল্ট থাকবে বর্তমান পদ্ধতিই, যাতে existing ব্যবহারকারীদের কিছু হঠাৎ বদলে না যায়।
   - যেখানে যেখানে তিথি/সংক্রান্তি হিসাব হয় (ক্যালেন্ডার, শুভ মুহূর্ত, উৎসব সব জায়গায়) — সবগুলোকে এই টগল অনুযায়ী দুই পদ্ধতির যেকোনো একটা ব্যবহার করতে হবে, তাই এটা এক জায়গায় বদলালেই হয় না, পুরো অ্যাপ জুড়ে টেস্ট করা লাগবে।

### Phase 3 — বড় কাজ, hardware + একাধিক প্যাকেজ (সবার শেষে)
6. **Live Astronomical Sky (#২)** — প্রথমে ক্যামেরা ছাড়াই একটা "sky map" স্ক্রিন বানানো ভালো: GPS + সময় দিয়ে existing গ্রহ-পজিশন গণিত (`sunLongitude`, `rashiIndexFor` ইত্যাদি) ব্যবহার করে ২D আকাশ-মানচিত্রে চাঁদ/গ্রহ বসানো, তারপর `sensors_plus` দিয়ে ফোন ঘোরালে ভিউ ঘোরানো যোগ করা।
7. **AR Sky (#১)** — এটা #২-এর ওপরেই তৈরি হবে: sky-map-এর গণিত + কম্পাস অংশ প্রমাণিত হয়ে গেলে, তার ওপর ক্যামেরা প্রিভিউ (`camera` প্যাকেজ) background হিসেবে বসিয়ে দিলেই "AR" অনুভূতি আসবে — সম্পূর্ণ ARCore/ARKit anchoring ছাড়াই। এতে খরচ ও ঝুঁকি অনেক কম, কিন্তু ফলাফল প্রায় একই রকম দেখাবে।

---

## নিরাপত্তা — কী কী ছোঁয়া হবে না

- **Panchang calculation**: বর্তমান `PanchangCalculator`-এর কোনো existing method/output বদলাবে না — সূর্য সিদ্ধান্ত শুধু *নতুন, পাশাপাশি* একটা অপশন হবে, ডিফল্ট আচরণ অপরিবর্তিত থাকবে।
- **GPS/Weather**: `LocationService`/`WeatherService` স্পর্শ করা হবে না — নতুন ফিচারগুলো (sky map, AR) এদের থেকে শুধু রিড করবে।
- **Animation/UI**: `CosmicBackground`, বর্তমান স্ক্রিন লেআউট অপরিবর্তিত থাকবে — নতুন ফিচার নতুন স্ক্রিন হিসেবে সাইড মেনুতে যোগ হবে, existing স্ক্রিন ওভাররাইট হবে না।
- **Backend/Database**: এই মুহূর্তে শুরু হবে না — সবকিছু লোকাল (SharedPreferences/লোকাল হিসাব) থাকবে, যেমন এখন আছে।
- প্রতিটা পরিবর্তনের পরেই যথারীতি bracket-balance চেক করে তারপর দেওয়া হবে, আর একসাথে অনেক ফিচার একসাথে বসানো হবে না — একটা একটা করে, প্রতিটার পর টেস্ট করার সুযোগ থাকবে।

---

## সংক্ষেপে অগ্রাধিকার-ক্রম

১. উৎসব কাউন্টডাউন + শেয়ার (৮) → ২. পঞ্চাঙ্গ শেয়ার কার্ড (১০) → ৩. মুহূর্ত ফাইন্ডারে ডেডলাইন ফিল্টার (৪) → ৪. ভয়েস রিমাইন্ডার (৯) → ৫. দুই পঞ্জিকা পদ্ধতি (৩) → ৬. Live Astronomical Sky (২) → ৭. AR Sky (১)

কোন ফিচারটা দিয়ে শুরু করতে চান বললে সেটা নিয়েই কাজ শুরু করে দেব।
